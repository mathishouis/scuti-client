import { createStore } from "vuex";
import Loading from "@/store/modules/loading";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    Loading,
  },
});
